---
import { cv } from '@/data/data';
import { Icon } from 'astro-icon/components';
import SectionTitle from '@/components/SectionTitle.astro';
import SectionWrapper from '@/components/SectionWrapper.astro';

const {
   basics: { name, level, label },
} = cv;
---

<SectionWrapper id="sobremi">
   <SectionTitle title="Sobre Mí" />
   <h3 class="md:text-3xl text-xl text-primary mb-4 text-center md:text-start">
      ¡Hola! Soy <span class="text-third">{name}</span>
      <span class="block text-base font-bold text-secondary italic mt-2"
         >{`${level} & ${label}`}</span
      >
   </h3>

   <div class="space-y-4 text-center md:text-start">
      <p class="md:text-lg leading-relaxed text-secondary">
         Me especializo en crear aplicaciones web modernas con enfoque en
         rendimiento y usabilidad, escribiendo código limpio y mantenible.
         Disfruto del trabajo en equipo y enfrentar desafíos que me permitan
         seguir creciendo profesionalmente.
      </p>
   </div>

   <div class="mt-12 flex md:justify-between flex-col md:flex-row gap-4">
      <div>
         <div
            class="flex items-center gap-2 mb-4 text-primary justify-center md:justify-start"
         >
            <Icon name="user" />
            <h4>Habilidades Interpersonales</h4>
         </div>
         <div class="flex flex-wrap gap-2">
            {
               [
                  'Trabajo en Equipo',
                  'Aprendizaje Continuo',
                  'Comunicación Efectiva',
               ].map((skill) => (
                  <span class="px-4 py-2 rounded-lg transition-all duration-300 hover:shadow-md cursor-pointer   bg-background-card text-primary border border-border hover:bg-third/10">
                     {skill}
                  </span>
               ))
            }
         </div>
      </div>
   </div>
</SectionWrapper>
